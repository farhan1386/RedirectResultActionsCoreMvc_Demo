@model RedirectResultActionsCoreMvc_Demo.Models.Department

@{
    ViewData["Title"] = "Details";
}

<div>
    <h4 class="text-uppercase">Department Details</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-sm btn-primary rounded-0">Edit</a>
    <a asp-action="Index" class="btn btn-sm btn-primary rounded-0">Back to List</a>
</div>
